# Decision assets

`ribbon/` contains the six viewport comparisons used to choose the shipped
header/footer ribbon in PR #25. They are retained in `main` so the merged PR's
visual rationale stays available after deleting its temporary orphan
`screens/21-ribbon` branch. These files are documentation only and are not
included in the Astro build.
